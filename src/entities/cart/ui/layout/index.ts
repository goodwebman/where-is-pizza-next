import { CartContainer } from './cart-container/cart-container';
import { CartItem } from './cart-item/cart-item';
import { CartItemSkeleton } from './cart-item/cart-item-skeleton';

export const CartLayout = {
  CartContainer,
  CartItem,
  CartItemSkeleton,
};
