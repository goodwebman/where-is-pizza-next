import { ButtonVariant } from '../buttons/types';

export const SPINNER_COLORS: Record<ButtonVariant, string> = {
  primary: 'var(--spinner-primary)',
  secondary: 'var(--spinner-primary)',
  danger: 'var(--spinner-primary)',
  ghost: 'var(--spinner-ghost)',
};